    public boolean findSmallInt(int value) {
    	int index;
    	for (index = 0; index < Rows; index++) {
        	if (table.get(index).smallInt == value) {
        		break;
        	}
        }
    	return (index != Rows);	
    }

