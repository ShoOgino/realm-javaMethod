     public boolean addIndex() {
    	 try {
	    	 stmt = db.prepare("CREATE INDEX i1a ON t1(indexInt);", true);
	    	 stmt.reset();
		     stmt.step();
		     stmt.dispose();
	    	 return true;
    	 } catch (SQLiteException e) {
    		 error(e);
	  	}
    	return false;
    }

