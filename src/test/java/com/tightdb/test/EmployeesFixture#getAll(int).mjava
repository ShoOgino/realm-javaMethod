	public static Object[] getAll(int index) {
		Object[] values = new Object[EMPLOYEE.length];

		for (int i = 0; i < values.length; i++) {
			values[i] = EMPLOYEE[i].get(index);
		}

		return values;
	}

