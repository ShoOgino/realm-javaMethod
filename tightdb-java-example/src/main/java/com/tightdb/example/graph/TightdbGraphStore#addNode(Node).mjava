	long addNode(Node node) {
		WriteTransaction tr = db.beginWrite();
		try {						
			tr.getTable(NODES);
			tr.commit();
		} catch (Exception e) {
			System.err.println("Exception");
			tr.rollback();
		}
		
		return 0;
	}

