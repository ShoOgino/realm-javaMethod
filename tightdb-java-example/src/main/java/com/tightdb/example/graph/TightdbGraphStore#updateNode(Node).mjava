	boolean updateNode(Node node) {
		WriteTransaction tr = db.beginWrite();
		try {						
			// TODO
			tr.commit();
		} catch (Exception e) {
			System.err.println("Exception");
			tr.rollback();
		}
		
		return true;
	}

