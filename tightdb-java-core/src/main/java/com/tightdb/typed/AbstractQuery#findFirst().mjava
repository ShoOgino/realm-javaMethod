	public Cursor findFirst() {
		// TODO: needed
		TableView viewBase = query.findAll(0, Table.INFINITE, 1);
		if (viewBase.size() > 0) {
			return cursor(viewBase, 0);
		} else {
			return null;
		}
	}

