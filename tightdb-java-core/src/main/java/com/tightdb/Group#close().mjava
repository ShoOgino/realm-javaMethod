    // If close() is called, no penalty is paid for delayed disposal
    // via the context
    public void close() {
        if (nativePtr != 0) {
            nativeClose(nativePtr);
            nativePtr = 0;
        }
    }

