    public void close() {
        synchronized (CloseMutex.getInstance()) {
            if (nativePtr != 0) {
                nativeClose(nativePtr);
                nativePtr = 0;
            }
        }
    }

