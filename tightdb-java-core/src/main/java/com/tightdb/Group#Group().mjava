    public Group() {
        context = new Context();
        this.nativePtr = createNative();
        try {
            checkNativePtr();
        } catch (RuntimeException e) {
            nativeClose(nativePtr);
            throw e;
        }
    }

