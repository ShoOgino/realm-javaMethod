    @Override
    public void close(){
        if (DEBUG) 
            System.err.println("==== TableView CLOSE, ptr= " + nativePtr);
        if (nativePtr != 0) {
            nativeClose(nativePtr);
            nativePtr = 0;
        }
    }

