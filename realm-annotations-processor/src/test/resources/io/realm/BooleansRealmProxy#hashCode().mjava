    @Override
    public int hashCode() {
        int result = 17;
        result = 31 * result + (isDone() ? 1 : 0);
        result = 31 * result + (isReady() ? 1 : 0);
        result = 31 * result + (ismCompleted() ? 1 : 0);
        return result;
    }

