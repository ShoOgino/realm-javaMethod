    @Override
    public void setColumnObject(AllTypes value) {
        if (value == null) {
            row.nullifyLink(columnIndices.get("columnObject"));
            return;
        }
        row.setLink(columnIndices.get("columnObject"), value.row.getIndex());
    }

