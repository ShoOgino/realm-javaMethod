    @Override
    public AllTypes getColumnObject() {
        if (row.isNullLink(columnIndices.get("columnObject"))) {
            return null;
        }
        return realm.get(some.test.AllTypes.class, row.getLink(columnIndices.get("columnObject")));
    }

