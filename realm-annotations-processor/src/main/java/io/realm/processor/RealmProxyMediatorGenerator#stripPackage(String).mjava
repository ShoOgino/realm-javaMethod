    private String stripPackage(String clazz) {
        String[] parts = clazz.split("\\.");
        if (parts.length > 0) {
            return parts[parts.length - 1];
        } else {
            return clazz;
        }
    }

