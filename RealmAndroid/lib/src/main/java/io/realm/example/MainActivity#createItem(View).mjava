    public void createItem(View v) {

        User user = new User();

        user.setId(0);
        user.setName("Username " + this.users.size());
        user.setEmail("");

        this.adapter.add(user);
    }

