    public void createItem(View v) {

        Realm wrRealm = new Realm(this);
        wrRealm.beginWrite();

        User user = new User();

        user.setId(0);
        user.setName("Username " + this.users.size());
        user.setEmail("");

        wrRealm.add(user);
        wrRealm.commit();


    }

