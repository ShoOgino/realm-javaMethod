    public long findIntWithIndex(long value) 
    {
        try {
            stmt.reset();
            stmt.bind(1, value);
            if (stmt.step())
                return (value);
            else
                return -1;
        } catch (SQLiteException e) {
            error(e);
        }
        return -1;
    }

