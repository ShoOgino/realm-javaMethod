    public boolean findLongInt(long value) {
        int index;
        for (index = 0; index < Rows; index++) {
            if (table.get(index).longInt == value) {
                break;
            }
        }
        return (index != Rows);
    }

