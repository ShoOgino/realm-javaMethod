    public boolean findSmallInt(long value) {
        try {
            stmt.reset();
            return stmt.step();
        } catch (SQLiteException e) {
            error(e);
        }
        return false;
    }

