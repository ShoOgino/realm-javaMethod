            public void bind(Person person) {
                this.person = person;
                name.setText(person.getName());
                age.setText(String.valueOf(person.getAge()));
            }

