    @Override
    public void onStop() {
        super.onStop();
        // Remember to close the Realm instance when done with it.
        dotsView.setRealmResults(null);
        realm.close();
    }

