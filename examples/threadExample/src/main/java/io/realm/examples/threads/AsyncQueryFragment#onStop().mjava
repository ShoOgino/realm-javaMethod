    @Override
    public void onStop() {
        super.onStop();
        // Remember to close the Realm instance when done with it.
        cancelRequest();
        cancelTransaction();
        realm.close();
    }

