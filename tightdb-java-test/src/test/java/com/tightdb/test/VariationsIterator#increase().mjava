    private void increase() {
        for (int i = 0; i < indexes.length; i++) {
            indexes[i] = (indexes[i] + 1) % sizes[i];
            if (indexes[i] > 0) {
                hasNext = true;
                return;
            }
        }
        hasNext = false;
    }

