    @BeforeMethod
    public void init() {
        deleteFile(testFile);
        deleteFile(testFile + ".lock"); // Make sure lock file is also removed
        
        db = new SharedGroup(testFile);
    }

