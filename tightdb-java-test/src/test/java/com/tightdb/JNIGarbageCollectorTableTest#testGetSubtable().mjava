    @Test
    public void testGetSubtable(){

        t = new Table();

        t.addColumn(ColumnType.TABLE, "table");
        t.addEmptyRow();


        long count = 10000;

        long loop = 0;

        while (true){
            System.out.println("Loop: " + loop);
            test1(count);
            test2(count);
           // test3(count);
            loop++;
        }
    }

