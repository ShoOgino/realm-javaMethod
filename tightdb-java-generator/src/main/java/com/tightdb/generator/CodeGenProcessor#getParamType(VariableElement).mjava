    private String getParamType(VariableElement field) {
        String type = fieldType(field);

        if (isSubtable(type)) {
            type = "Object[][]";
        }

        return type;
    }

