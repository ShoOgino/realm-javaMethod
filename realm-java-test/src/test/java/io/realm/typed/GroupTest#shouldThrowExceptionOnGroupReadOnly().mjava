    @Test
    public void shouldThrowExceptionOnGroupReadOnly() throws IOException {
        new File(FILENAME).delete();
        // Throw when opening non-existing file
        try { Group group = new Group(FILENAME, Group.OpenMode.READ_ONLY); fail("Group is read only"); } catch (io.realm.IOException e) { }
    }

