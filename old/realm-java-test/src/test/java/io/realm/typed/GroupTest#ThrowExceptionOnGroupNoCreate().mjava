    @Test
    public void ThrowExceptionOnGroupNoCreate() throws IOException {
        new File(FILENAME).delete();
        try { Group group2 = new Group(FILENAME, Group.OpenMode.READ_WRITE_NO_CREATE); fail("Exception excpected"); } catch (io.realm.IOException e) { }
    }

