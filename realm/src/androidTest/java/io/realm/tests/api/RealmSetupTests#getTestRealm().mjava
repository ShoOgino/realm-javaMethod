    protected Realm getTestRealm() throws IOException {
        setupSharedGroup();
        
        Realm testRealm = new Realm(getContext().getFilesDir());
        testRealm.clear();
        return testRealm;
    }

