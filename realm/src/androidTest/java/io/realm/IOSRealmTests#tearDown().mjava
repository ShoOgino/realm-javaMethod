    @Override
    protected void tearDown() throws Exception {
        super.tearDown();
        Realm.removeDefaultConfiguration();
        if (realm != null) {
            realm.close();
        }
    }

