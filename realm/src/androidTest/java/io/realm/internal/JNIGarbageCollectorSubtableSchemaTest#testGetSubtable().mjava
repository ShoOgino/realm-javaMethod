    public void testGetSubtable(){

        long count = 10; // 1000;
        long loop = 10; //  1000;

        for (int i=0;i<loop;i++){
            t1(count);
            t2(count);
            t3(count);
        }
    }

