    public Object[] next() {
        Object[] result = new Object[lists.size()];

        for (int i = 0; i < lists.size(); i++) {
            result[i] = lists.get(i).get(indexes[i]);
        }

        increase();
        return result;
    }

