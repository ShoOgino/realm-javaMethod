    @Override
    public void run() {
        Realm realm = realmRef.get();
        if(realm != null && realm.runEventHandler) {
            if (realm.hasChanged()) {
                realm.sendNotifications();
            }
        }
    }

