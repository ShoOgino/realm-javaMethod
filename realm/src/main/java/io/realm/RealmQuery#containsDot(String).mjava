    private boolean containsDot(String s) {
        int i;
        int n;

        i = 0;
        n = s.length();
        while (i < n) {
            if (s.charAt(i) == '.')
                return true;
            i++;
        }
        return false;
    }

