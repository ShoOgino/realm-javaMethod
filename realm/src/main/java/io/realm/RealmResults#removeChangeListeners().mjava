    /**
     * Remove all registered listeners.
     */
    public void removeChangeListeners() {
        if (realm != null) {
            realm.checkIfValid();
        }
        listeners.clear();
    }

