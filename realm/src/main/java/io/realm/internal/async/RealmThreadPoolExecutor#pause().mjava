    public void pause() {
//        ((PausableArrayBlockingQueue<Runnable>) getQueue()).pause();
        pauseLock.lock();
        try {
            isPaused = true;
        } finally {
            pauseLock.unlock();
        }
    }

