            @Override
            public QueryUpdateTask build() {
                return new QueryUpdateTask(realmResultsEntries != null,
                        realmConfiguration,
                        realmResultsEntries,
                        realmObjectEntry,
                        callerHandler,
                        message);
            }

