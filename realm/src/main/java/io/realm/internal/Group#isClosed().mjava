    /**
     * Checks if a group has been closed and can no longer be used.
     *
     * @return True if closed, false otherwise.
     */
    boolean isClosed() {
        return nativePtr == 0;
    }

