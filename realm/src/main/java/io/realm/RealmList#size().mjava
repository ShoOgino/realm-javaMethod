    /**
     * {@inheritDoc}
     */
    @Override
    public int size() {
        if (managedMode) {
            return ((Long)view.size()).intValue();
        } else {
            return nonManagedList.size();
        }
    }

