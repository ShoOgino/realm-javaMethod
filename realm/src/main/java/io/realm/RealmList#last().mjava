    /**
     * Find the last object
     * @return The last object
     */
    public E last() {
        if (!view.isEmpty()) {
            return get(size()-1);
        }
        return null;
    }

