    /**
     * How many items are in the data set.
     *
     * @return Count of items.
     */
    @Override
    public int getCount() {
        if (realmResults == null) {
            return 0;
        }
        return realmResults.size();
    }

