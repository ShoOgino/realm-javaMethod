    public void close() {
        if (nativePtr != 0) {
            Set<RealmObjectSchema> schemas = getAll();
            for (RealmObjectSchema schema : schemas) {
                schema.close();
            }
            nativeClose(nativePtr);
        }
    }

