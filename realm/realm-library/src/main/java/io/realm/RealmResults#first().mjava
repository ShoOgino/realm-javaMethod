    /**
     * {@inheritDoc}
     */
    @Override
    public E first() {
        if (size() > 0) {
            return get(0);
        } else {
            throw new IndexOutOfBoundsException("No results were found.");
        }
    }

