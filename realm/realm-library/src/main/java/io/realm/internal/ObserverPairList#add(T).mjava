    public void add(T pair) {
        if (!pairs.contains(pair)) {
            pairs.add(pair);
            pair.removed = false;
        }
        if (cleared) {
            cleared = false;
        }
    }

