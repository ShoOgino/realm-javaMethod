        void add(NativeObjectReference ref) {
            if (pool.size() <= ref.refIndex) {
                pool.add(ref);
            } else {
                pool.set(ref.refIndex, ref);
            }
        }

