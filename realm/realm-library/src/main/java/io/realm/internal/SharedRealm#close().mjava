    @Override
    public void close() {
        synchronized (context) {
            if (nativePtr != 0) {
                nativeCloseSharedRealm(nativePtr);
                nativePtr = 0;
            }
        }
    }

