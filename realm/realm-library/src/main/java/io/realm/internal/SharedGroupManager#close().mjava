    /**
     * Closes the underlying {@link SharedGroup} and free any native resources.
     */
    @Override
    public void close() {
        sharedGroup.close();
        sharedGroup = null;
        transaction = null;
    }

