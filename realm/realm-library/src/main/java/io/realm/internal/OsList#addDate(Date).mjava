    public void addDate(@Nullable Date value) {
        if (value == null) {
            nativeAddNull(nativePtr);
        } else {
            nativeAddDate(nativePtr, value.getTime());
        }
    }

