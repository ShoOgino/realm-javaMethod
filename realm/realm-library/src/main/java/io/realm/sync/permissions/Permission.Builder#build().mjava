        /**
         * Creates the unmanaged {@link Permission} object.
         */
        public Permission build() {
            return new Permission(
                    role,
                    canRead,
                    canUpdate,
                    canDelete,
                    canSetPermissions,
                    canQuery,
                    canCreate,
                    canModifySchema
            );
        }

