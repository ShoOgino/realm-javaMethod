    public long expiresMs() {
        long expiresMs = expires * 1000;
        if (expiresMs < expires) {
            return Long.MAX_VALUE; // Overflow
        } else {
            return expiresMs;
        }
    }

