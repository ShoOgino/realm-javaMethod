    public void clearTokens() {
        realms.clear();
        refreshToken = null;
        if (refreshTask != null) {
            refreshTask.cancel();
            refreshTask = null;
        }
    }

