    /**
     * {@inheritDoc}
     */
    @Override
    public User load(String key) {
        String userData = sp.getString(key, "");
        if (userData.equals("")) {
            return null;
        }
        return User.fromJson(userData);
    }

