    /**
     * {@inheritDoc}
     */
    @Override
    public void deleteFromRealm(int location) {
        realm.checkIfValid();
        TableOrView table = getTableOrView();
        table.remove(location);
    }

