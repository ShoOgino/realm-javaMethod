    /**
     * {@inheritDoc}
     */
    @Override
    public void deleteFromRealm(int location) {
        realm.checkIfValid();
        TableOrView table = getTable();
        table.remove(location);
    }

