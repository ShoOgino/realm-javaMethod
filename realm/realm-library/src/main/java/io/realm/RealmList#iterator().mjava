    /**
     * {@inheritDoc}
     */
    @Override
    public Iterator<E> iterator() {
        if (managedMode) {
            return new RealmItr();
        } else {
            return super.iterator();
        }
    }

