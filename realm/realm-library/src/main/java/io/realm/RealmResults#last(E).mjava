    /**
     * {@inheritDoc}
     */
    @Override
    public E last(E defaultValue) {
        int size = size();
        if (size > 0) {
            return get(size - 1);
        } else {
            return defaultValue;
        }
    }

