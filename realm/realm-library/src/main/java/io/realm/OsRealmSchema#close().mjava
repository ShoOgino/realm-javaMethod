    // See BaseRealm uses a StandardRealmSchema, not a OsRealmSchema.
    @Override
    public void close() {
        if (nativePtr != 0L) {
            nativeClose(nativePtr);
            nativePtr = 0L;
        }
    }

