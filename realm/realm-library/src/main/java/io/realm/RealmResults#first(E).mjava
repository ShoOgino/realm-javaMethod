    /**
     * {@inheritDoc}
     */
    @Override
    public E first(E defaultValue) {
        if (size() > 0) {
            return get(0);
        } else {
            return defaultValue;
        }
    }

