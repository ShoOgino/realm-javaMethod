    /**
     * {@inheritDoc}
     */
    @Override
    public boolean deleteAllFromRealm() {
        baseRealm.checkIfValid();
        if (size() > 0) {
            osResults.clear();
            return true;
        }
        return false;
    }

