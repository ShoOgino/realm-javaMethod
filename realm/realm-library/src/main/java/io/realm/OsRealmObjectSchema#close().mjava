    @Override
    public void close() {
        Set<Property> properties = copyProperties();
        for (Property property : properties) {
            property.close();
        }
        nativeClose(nativePtr);
    }

