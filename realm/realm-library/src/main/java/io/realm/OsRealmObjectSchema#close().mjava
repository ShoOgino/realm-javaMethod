    @Override
    public void close() {
        Set<Property> properties = getProperties();
        for (Property property : properties) {
            property.close();
        }
        nativeClose(nativePtr);
    }

