    private RealmResults(BaseRealm realm, String className, long nativePtr) {
        this.realm = realm;
        this.className = className;
        this.query = null;
        collection = null;
    }

