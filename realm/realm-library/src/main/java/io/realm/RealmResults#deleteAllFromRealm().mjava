    /**
     * {@inheritDoc}
     */
    @Override
    public boolean deleteAllFromRealm() {
        realm.checkIfValid();
        if (size() > 0) {
            TableOrView table = getTableOrView();
            table.clear();
            return true;
        } else {
            return false;
        }
    }

