        @Override
        public void close() {
            for (Map.Entry<String, OsRealmObjectSchema> entry : schema.entrySet()) {
                entry.getValue().close();
            }
            schema.clear();
        }

