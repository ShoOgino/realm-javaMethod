        @Override
        public void close() {
            for (Map.Entry<String, RealmObjectSchema> entry : schema.entrySet()) {
                entry.getValue().close();
            }
            schema.clear();
        }

