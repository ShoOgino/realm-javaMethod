    public void testGetSubtable(){

        t = new Table();
        t.addColumn(RealmFieldType.UNSUPPORTED_TABLE, "table");

        long count = 100;
        long loop = 100;

        for (int i=0;i<loop;i++){
            t1(count);
            t2(count);
            t3(count);
        }
    }

