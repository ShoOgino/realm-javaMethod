    public void testGetSubtableView(){
        
        t = new Table();
        t.addColumn(ColumnType.TABLE, "table");

        long count = 10; // 1000;
        long loop = 100;

        for (int i=0;i<loop;i++){
            t1(count);
            t2(count);
            t3(count);
        }
    }

