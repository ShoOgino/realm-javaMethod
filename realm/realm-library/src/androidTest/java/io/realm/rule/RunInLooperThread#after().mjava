    @Override
    protected void after() {
        super.after();

        // probably belt *and* suspenders...
        synchronized (lock) {
            backgroundHandler = null;
            keepStrongReference = null;
        }
    }

