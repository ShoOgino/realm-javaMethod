    @Nullable
    private static SyncUser toSyncUserOrNull(@Nullable String userJson) {
        if (userJson == null) {
            return null;
        }
        return SyncUser.fromJson(userJson);
    }

