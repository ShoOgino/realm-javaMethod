    @Override
    public int hashCode() {
        int result = identity.hashCode();
        result = 31 * result + (isAdmin ? 1 : 0);
        result = 31 * result + metadata.hashCode();
        return result;
    }

