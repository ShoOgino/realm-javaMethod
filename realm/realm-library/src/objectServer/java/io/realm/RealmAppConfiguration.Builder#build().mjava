        public RealmAppConfiguration build() {
            return new RealmAppConfiguration(appId,
                    appName,
                    appVersion,
                    baseUrl,
                    context,
                    defaultErrorHandler,
                    encryptionKey,
                    logLevel,
                    requestTimeoutMs);
        }

