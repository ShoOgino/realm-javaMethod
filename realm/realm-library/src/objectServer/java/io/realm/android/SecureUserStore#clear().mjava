    @Override
    public void clear() {
        Set<String> all = sp.getAll().keySet();
        SharedPreferences.Editor editor = sp.edit();
        for (String key : all) {
            editor.remove(key);
        }
        editor.apply();
    }

