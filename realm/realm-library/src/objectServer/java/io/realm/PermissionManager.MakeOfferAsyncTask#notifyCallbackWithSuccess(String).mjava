        void notifyCallbackWithSuccess(String token) {
            try {
                callback.onSuccess(token);
            } finally {
                activeTasks.remove(this);
            }
        }

