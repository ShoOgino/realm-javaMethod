    @After
    public void tearDown() {
        if (realm != null && !realm.isClosed()) {
            realm.close();
        }
        if (app != null) {
            RealmAppExtKt.close(app);
        }
    }

